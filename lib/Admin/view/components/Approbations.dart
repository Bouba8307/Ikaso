import 'package:flutter/material.dart';

class ApprobationsPage extends StatelessWidget {
  const ApprobationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color.fromARGB(255, 206, 47, 47),
    );
  }
}
